name: CI

on:
  push:
    branches: [main, "claude/**"]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # The probes are platform code - procfs, psapi, waitid, GetExitCodeProcess,
    # cgroup counters - and the Windows and macOS paths cannot be exercised on
    # a Linux runner at all. That is the whole reason this matrix exists.
    name: ${{ matrix.os }} / py${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.9", "3.11", "3.13"]
        exclude:
          # setup-python has no 3.9 build for the arm64 macOS runners, and the
          # x86_64 macos-13 runners that had one are retired - a job asking for
          # them queues forever. 3.9 is covered on Linux and Windows.
          - os: macos-latest
            python-version: "3.9"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      # Deliberately without psutil: that is the configuration most people
      # actually have, and every probe is supposed to degrade to a declared
      # "unavailable" rather than to a wrong number.
      - name: Install without psutil
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Show what this machine can measure
        run: |
          python -c "import json; from pytest_failure_instrumentation import probes; print(json.dumps(probes.capabilities(), indent=2))"

      - name: Test
        run: pytest -q

  with-psutil:
    # psutil is never required, only ever an upgrade - so it gets its own job
    # rather than being folded into the matrix and hiding the default.
    name: ${{ matrix.os }} / psutil
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install with psutil
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test,psutil]"

      - name: Show what this machine can measure
        run: |
          python -c "import json; from pytest_failure_instrumentation import probes; print(json.dumps(probes.capabilities(), indent=2))"

      - name: Test
        run: pytest -q

  without-xdist:
    # Two of the five kinds are not distributed problems, and the plugin has to
    # register without xdist installed - where pytest_testnodedown has no
    # hookspec at all, and an unspecced hookimpl is a registration error.
    name: ubuntu / no xdist
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install without pytest-xdist
        run: |
          python -m pip install --upgrade pip
          pip install -e .

      - name: Test
        run: pytest -q
