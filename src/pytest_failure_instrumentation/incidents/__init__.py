"""Building incidents and raising them through the hook.

One module per kind: the model that describes it, and the builder that gathers
it. :mod:`.registry` is the union of every kind that can arrive.
"""
