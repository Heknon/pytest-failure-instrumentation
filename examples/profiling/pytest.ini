[pytest]
testpaths = tests
pythonpath = .
addopts = --failure-instrumentation --failure-profile -p no:cacheprovider
failure_packages = demo_product
failure_directory = .pytest-failures
failure_profile_peak_mb = 1000
